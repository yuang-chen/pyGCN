import math

import torch

from torch.nn.parameter import Parameter
from torch.nn.modules.module import Module


class GraphConvolution(Module):
    """
    Simple GCN layer, similar to https://arxiv.org/abs/1609.02907
    """

    def __init__(self, in_features, out_features, bias=True):
        super(GraphConvolution, self).__init__()
        self.in_features = in_features
        self.out_features = out_features
        self.weight = Parameter(torch.FloatTensor(in_features, out_features))
        """ ^^^
        initialize the weights
        """
        if bias:
            self.bias = Parameter(torch.FloatTensor(out_features))
        else:
            self.register_parameter('bias', None)
        self.reset_parameters()

    def reset_parameters(self):
        stdv = 1. / math.sqrt(self.weight.size(1))
        self.weight.data.uniform_(-stdv, stdv)
        if self.bias is not None:
            self.bias.data.uniform_(-stdv, stdv)

    def forward(self, input, adj):
        support = torch.mm(input, self.weight)
        """ ^^^
        input X weights -- dense_tensor X dense_tensor
        """
        output = torch.spmm(adj, support)
        """ ^^^
        adj X (input X weight) -- sparse_tensor X (dense_tensor), therefore the spmm() is used
        sparse_tensor: a sparse representation of the same tensor that focuses only on the non-zero values
        dense_tensor: the "matrix" (normal) representation 
        """
        if self.bias is not None:
          
            return output + self.bias
        else:
            return output

    def __repr__(self):
        return self.__class__.__name__ + ' (' \
               + str(self.in_features) + ' -> ' \
               + str(self.out_features) + ')'


#net = GraphConvolution(10, 4, True)


